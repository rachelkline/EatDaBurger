<div class="card">
    <div class="card-header text-center">
        <h4>Devoured</h4>
    </div>
    <div class="card-body">

        {{#each devouredList}}
        <div>
            <p>{{id}} {{burger_name}}
                <button class="deleteburger btn btn-sm btn-outline-danger m-1 devour-btn float-right"
                            data-id="{{id}}">DELETE
                    </button>
            </p>
          </div>  
        {{/each }}

    </div>
</div>